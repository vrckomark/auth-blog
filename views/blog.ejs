<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./styles/style.css" />
    <title>Blog</title>
  </head>
  <body class="bg-background">
    <%- include("navbar", session) %>
    <div class="w-screen mt-12 flex flex-col items-center content-center">
      <div
        class="text-primary justify-center w-1/2 py-4 rounded-xl mb-10 flex text-4xl"
      >
        <%= blog.title %>
      </div>
      <div
        class="text-gray-500 rounded-xl p-2 justify-between flex w-1/2 text-left text-xl"
      >
        <div>
          <% date = new Date(blog.date) %>Posted on <%= date.getDate() %>/<%=
          date.getMonth()+1 %>/<%= date.getFullYear() %> at <%=
          date.getUTCHours()-(date.getTimezoneOffset()/60) %>:<%
          if(date.getMinutes()<10){ %>0<%= date.getMinutes() %><% } else { %><%=
          date.getMinutes() %><% } %>
        </div>
        <div class="text-primary text-2xl"><%= blog.author %></div>
      </div>
      <div
        class="text-secondary rounded-xl p-4 border-2 border-primary flex w-1/2 text-left text-2xl"
      >
        <%= blog.content %>
      </div>
    </div>
  </body>
</html>
