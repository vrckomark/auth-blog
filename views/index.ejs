<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./styles/style.css" />
    <title>Blogs</title>
  </head>
  <body class="bg-gray-800">
    <% if(session.isAuth){ %>
    <a
      href="/blog/create"
      class="text-3xl bottom-0 right-0 p-10 fixed text-purple-500"
      >Create a blog</a
    >
    <% } else {%>
    <a
      href="/register"
      class="text-3xl bottom-0 right-0 p-10 fixed text-purple-500"
    >
      Sign up to create a blog
    </a>
    <% } %> <%- include("navbar", { session }) %>
    <div class="p-10 flex flex-col w-screen justify-center items-center">
      <% blogs.map((blog)=>{ %>
      <div class="mb-5 w-1/2">
        <div class="text-xl text-purple-500"><%= blog.date %></div>
        <a href="/blog/<%= blog.id %> " class="text-3xl my-4 text-purple-500"
          ><%= blog.title %></a
        >
        <div class="text-xl text-purple-500 w-1/2"><%= blog.content %></div>
      </div>

      <% }) %>
    </div>
  </body>
</html>
